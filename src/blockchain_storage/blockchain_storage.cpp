#include "blockchain_storage.hpp"

namespace BlockchainNode
{

    BlockchainStorage::BlockchainStorage()
    {
    }

    void BlockchainStorage::add_block(const Block &block)
    {
    }

    void BlockchainStorage::add_transaction(const Transaction &transaction)
    {
    }

}